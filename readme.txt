1. Clone this repo and go to /fastapi-todo folder
2. Run docker compose up
3. Open terminal and run "docker ps" then run "docker exec -it fastapi_container bash" 
4. Run command "alembic upgrade head"
5. Connect to database like the given variables in .env 
6. Go to fastapi swagger at localhost:8000
